//! The loopback HTTP API adapter. Read routes are open on loopback; mutations
//! require an auth header and CORS is restricted to localhost.

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {}
}
