# ---- Rust ----
/target
**/target/

# ---- Node / frontend ----
node_modules/
dist/
dist-ssr/
.vite/
*.local

# ---- Tauri ----
# build output lives in src-tauri/target → covered by **/target/
**/gen/schemas/

# ---- impeccable (design skill) runtime / generated ----
.impeccable/

# ---- Claude Code local (machine-specific) ----
.claude/settings.local.json
# session-local tooling artifacts (Forge Studio pipeline state); tracked: .claude/skills
.claude/sessions/
.claude/state/
.claude/evolution/
.claude/spec.md
.claude/features.json
# harness-generated skill lockfile — regenerated per session, never committed
skills-lock.json

# ---- OS / editor cruft ----
.DS_Store
Thumbs.db
*.swp
*~
.idea/

# ---- Solo reference material / local scratch (clean-room — never commit) ----
/solo.yml
/crates/solo.yml
/processes.webp
